/*
 * Automatically generated by:
 * $FreeBSD$
 */
#include <sys/param.h>
#include <sys/errno.h>
#include <sys/kernel.h>
#include <sys/module.h>
#include <sys/linker.h>
#include <sys/firmware.h>
#include <sys/systm.h>

static long realtek_license_ack = 0;
extern char _binary_rsu_rtl8712fw_fw_start[], _binary_rsu_rtl8712fw_fw_end[];

static int
rsu_rtl8712fw_fw_modevent(module_t mod, int type, void *unused)
{
	const struct firmware *fp, *parent;
	int error;
	switch (type) {
	case MOD_LOAD:


		TUNABLE_LONG_FETCH("legal.realtek.license_ack", &realtek_license_ack);
		if (!realtek_license_ack) {
			printf("rsu-rtl8712fw: You need to read the LICENSE file in /usr/share/doc/legal/realtek.LICENSE.\n");
			printf("rsu-rtl8712fw: If you agree with the license, set legal.realtek.license_ack=1 in /boot/loader.conf.\n");
			return(EPERM);
		}

		fp = firmware_register("rsu-rtl8712fw", _binary_rsu_rtl8712fw_fw_start , (size_t)(_binary_rsu_rtl8712fw_fw_end - _binary_rsu_rtl8712fw_fw_start), 120, NULL);
		if (fp == NULL)
			goto fail_0;
		parent = fp;
		return (0);
	fail_0:
		return (ENXIO);
	case MOD_UNLOAD:
		error = firmware_unregister("rsu-rtl8712fw");
		return (error);
	}
	return (EINVAL);
}

static moduledata_t rsu_rtl8712fw_fw_mod = {
        "rsu_rtl8712fw_fw",
        rsu_rtl8712fw_fw_modevent,
        0
};
DECLARE_MODULE(rsu_rtl8712fw_fw, rsu_rtl8712fw_fw_mod, SI_SUB_DRIVERS, SI_ORDER_FIRST);
MODULE_VERSION(rsu_rtl8712fw_fw, 1);
MODULE_DEPEND(rsu_rtl8712fw_fw, firmware, 1, 1, 1);

